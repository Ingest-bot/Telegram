name: Walla and Hamal News Bot

on:
  repository_dispatch:
    types: [my_script]
  workflow_dispatch: # מאפשר לך להריץ גם ידנית ליתר ביטחון
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # הוספתי כאן את requests שדרוש לחמ"ל
          pip install feedparser python-telegram-bot requests

      - name: Run bot
        env:
          # Secrets של וואלה (נשאר כפי שהיה)
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          # Secrets חדשים של חמ"ל
          HAMAL_TELEGRAM_TOKEN: ${{ secrets.HAMAL_TELEGRAM_TOKEN }}
          HAMAL_CHAT_ID: ${{ secrets.HAMAL_CHAT_ID }}
        run: python bot.py

      - name: Commit and Push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          # הוספתי כאן פקודה שיוצרת את הקובץ אם הוא לא קיים כדי למנוע שגיאות
          touch last_links.txt
          git add last_links.txt
          git commit -m "Update history [skip ci]" || echo "No changes to commit"
          git push
