name: News Bot Unified

on:
  push:
    branches:
      - main
  # מאפשר הפעלה מ-Cron-job.org דרך ה-URL הכללי /dispatches
  repository_dispatch:
    types: [run-bot]
  # מאפשר הפעלה ידנית מתוך ממשק GitHub
  workflow_dispatch:
  # אופציונלי: גיבוי פנימי של GitHub (פועל כל 20 דקות אם החיצוני נכשל)
  schedule:
    - cron: '*/20 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Cache pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser python-telegram-bot requests

      - name: Run Unified Bot
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          HAMAL_TELEGRAM_TOKEN: ${{ secrets.HAMAL_TELEGRAM_TOKEN }}
          HAMAL_CHAT_ID: ${{ secrets.HAMAL_CHAT_ID }}
        run: python bot.py

      - name: Commit and Push history changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          git add last_links.txt
          git stash || echo "No changes to stash"
          git pull --rebase origin main
          git stash pop || echo "No stash to pop"
          
          git add last_links.txt
          git commit -m "Update news history [skip ci]" || echo "No changes to commit"
          git push origin main
